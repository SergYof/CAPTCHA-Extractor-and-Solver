# TODO
* Feed the cut tiles into a computer vision API
* Extract the CAPTCHA instruction text and send it alongside the pictures
* Create a separate CSS for flashes
* Add password field in the registration form